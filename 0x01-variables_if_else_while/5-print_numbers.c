#include<stdio.h>

/**
 * main- This programm prints out the alphabet.
 *
 * Return -It always prints out 0 (success)
 */
int main(void)
	int n;
	for (n=0 ; n < 10 ; n++)
	{
		printf("%d", n) ;
	}
	putchar("\n");
	return(0);
